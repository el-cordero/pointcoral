% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{read_cpce_output_raw_tabs}
\alias{read_cpce_output_raw_tabs}
\title{Read \verb{_raw} sheets from a CPCe output workbook}
\usage{
read_cpce_output_raw_tabs(
  path,
  crosswalk = NULL,
  skip_deep_cres = TRUE,
  sheet_pattern = "_raw$"
)
}
\arguments{
\item{path}{Path to a CPCe output \code{.xls} or \code{.xlsx} workbook.}

\item{crosswalk}{Optional label crosswalk data frame or path. When \code{NULL},
the bundled example crosswalk is used. The raw CPCe label in the workbook
is joined to \code{raw_code} or \code{raw_label}.}

\item{skip_deep_cres}{Whether to skip worksheets whose names start with
\code{deep_cres_}. Those sheets use a different layout.}

\item{sheet_pattern}{Regular expression used to identify raw worksheets.
Defaults to sheets ending in \verb{_raw}.}
}
\value{
A tibble combining all selected raw worksheets. The original CPCe
raw major/group column is preserved as \code{cpce_major_category} when present.
}
\description{
CPCe output workbooks often contain one worksheet per image, with raw point
annotation worksheets named like \code{image_name_raw}. This function reads only
those raw worksheets, skips \code{deep_cres_..._raw} worksheets by default, keeps
the raw worksheet columns, adds \code{image_name} as the first column, and adds a
full \code{major_category} column using a label crosswalk.
}
\examples{
xlsx <- system.file(
  "extdata", "pointcoral_example_cpce_output_raw_tabs.xlsx",
  package = "pointcoral"
)
read_cpce_output_raw_tabs(xlsx)
}
