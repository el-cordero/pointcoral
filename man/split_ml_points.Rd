% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml.R
\name{split_ml_points}
\alias{split_ml_points}
\title{Split ML points into train/validation/test sets}
\usage{
split_ml_points(
  points,
  split_by = c("image", "transect", "site"),
  train = 0.7,
  val = 0.15,
  test = 0.15,
  seed = 1
)
}
\arguments{
\item{points}{A tidy point table.}

\item{split_by}{One of \code{"image"}, \code{"transect"}, or \code{"site"}.}

\item{train, val, test}{Split proportions.}

\item{seed}{Random seed.}
}
\value{
The input table with \code{split} and \code{split_unit} columns.
}
\description{
Assigns split labels by image, transect, or site to avoid leakage. Splits are
reproducible with \code{seed}.
}
\examples{
example_dir <- system.file("extdata", package = "pointcoral")
pts <- read_cpce_folder(example_dir, image_root = example_dir, recursive = FALSE)
split_ml_points(pts, split_by = "image", train = 0.5, val = 0, test = 0.5)
}
