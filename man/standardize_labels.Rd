% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{standardize_labels}
\alias{standardize_labels}
\title{Standardize CPCe labels with a crosswalk}
\usage{
standardize_labels(
  points,
  crosswalk,
  by = NULL,
  unknown_action = c("warn", "keep", "drop", "error")
)
}
\arguments{
\item{points}{A tidy point table.}

\item{crosswalk}{A crosswalk data frame, usually from \code{\link[=read_label_crosswalk]{read_label_crosswalk()}}.}

\item{by}{Optional join columns. Use a character vector for same-name joins
or a named vector where names are point-table columns and values are
crosswalk columns.}

\item{unknown_action}{How to handle labels not found in the crosswalk:
\code{"warn"} keeps rows and warns, \code{"keep"} keeps rows silently, \code{"drop"}
drops unmapped rows with a warning, and \code{"error"} stops.}
}
\value{
A tidy point table with standardized label columns.
}
\description{
Joins raw CPCe labels/codes to user-defined clean labels, ecological
categories, ML classes, and class IDs. Raw labels and raw codes are always
preserved.
}
\examples{
cpc <- system.file("extdata", "HIW_158_W_U-1.cpc", package = "pointcoral")
xwalk <- system.file(
  "extdata", "pointcoral_example_crosswalk.csv",
  package = "pointcoral"
)
pts <- read_cpce_file(cpc)
standardize_labels(pts, read_label_crosswalk(xwalk))
}
